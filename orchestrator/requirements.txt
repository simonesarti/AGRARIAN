docker==7.1.0
fastapi==0.115.12
httpx==0.28.1
# Both FlightRuntime backends ship in one image, selected by FLIGHT_RUNTIME. Each
# client is imported lazily inside its backend, so the unused one costs nothing at
# runtime — but the image has to carry both for the setting to be a setting.
kubernetes==31.0.0
# FastAPI's Form(...) needs this: MediaMTX's hooks post form-encoded bodies, not JSON.
python-multipart==0.0.20
uvicorn==0.34.2
